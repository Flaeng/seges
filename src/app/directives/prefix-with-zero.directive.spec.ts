import { PrefixWithZeroDirective } from './prefix-with-zero.directive';

describe('PrefixWithZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new PrefixWithZeroDirective();
    expect(directive).toBeTruthy();
  });
});
