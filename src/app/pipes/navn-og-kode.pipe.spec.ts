import { NavnOgKodePipe } from './navn-og-kode.pipe';

describe('NavnOgKodePipe', () => {
  it('create an instance', () => {
    const pipe = new NavnOgKodePipe();
    expect(pipe).toBeTruthy();
  });
});
